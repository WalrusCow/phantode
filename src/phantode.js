/*
 * Main driver file for phantode.
 */

var child_process = require('child_process');

function spawnPhantom() {
  /*
   * Spawn a new phantom process. Return an object that wraps the process
   * into a more useful interface
   */
}
